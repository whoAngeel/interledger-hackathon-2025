version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./src:/app/src
      - ./keys:/app/keys:ro
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev

  redis:
    ports:
      - "6379:6379"